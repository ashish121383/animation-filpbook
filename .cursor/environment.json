{
  "install": "chmod +x scripts/*.sh && ./scripts/setup.sh",
  "start": "sudo service postgresql start || true && sudo service redis-server start || true",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && source .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run build && PORT=5173 node server.mjs"
    },
    {
      "name": "celery",
      "command": "cd backend && source .venv/bin/activate && celery -A app.celery_app worker --loglevel=info --concurrency=2"
    }
  ]
}
